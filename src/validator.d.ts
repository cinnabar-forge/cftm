/**
 * Checks the markup data against the schema
 * @param {object} data markup data
 * @returns {boolean} true if the data compiles to the schema, false otherwise
 */
export function validateCinnabarMarkup(data: object): boolean;
